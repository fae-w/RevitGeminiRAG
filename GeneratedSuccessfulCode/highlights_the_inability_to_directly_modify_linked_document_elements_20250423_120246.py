# Purpose: This script highlights the inability to directly modify linked document elements.

﻿# Error: Cannot directly modify parameters of elements within a linked document using the Revit API from the host document. The linked document is opened in a read-only state when accessed via GetLinkDocument(). Modifications must be made in the linked document itself.